# Working material: specs, hackathon planning, experiment notes. Not shipped.
scratch/

# Rust
/target/
**/*.rs.bk

# Secrets and local config
.env
.env.*
!.env.example
*.key
*.pem

# Local Mooshik state (autobiography lives in SQLite/Postgres, never in the repo)
*.db
*.db-shm
*.db-wal
mooshik.db*

# Node sidecar / ADK ingester
node_modules/
__pycache__/
# Python build artefacts. `pip install ./<pkg>` leaves these in the source
# tree, and with four packages here they turn up wherever anyone installs one.
*.egg-info/
build/
.pytest_cache/
*.py[cod]
.venv/
venv/

# Ingester checkpoint state
.ingest/

# Editors and OS
.DS_Store
*.swp
.idea/
.vscode/


# Added by cargo

/target

# Live-run fixture repo (created by ingest-fixtures/make-git-fixture.sh)
ingest-fixtures/demo-repo/

# M9 live-run artifacts (regenerable: sample --seed 42)
measurement/sample.jsonl
measurement/sample.grades.jsonl
measurement/grades.tsv
